# Phase 07: Topology

## Contract

Use managed devices as explicit CDP/LLDP discovery seeds per group tree.
Normalize and deduplicate links, resolve managed peers without creating
unknown devices, persist discovery metadata, and render managed/unmanaged
nodes with pan, zoom, fit, labels, and navigation in Cytoscape.js.

## Outcome

`TopologyLink`, migration `0003`, discovery service/API, normalization tests,
and the Cytoscape topology canvas were delivered.
