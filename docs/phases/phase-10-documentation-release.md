# Phase 10: Documentation And Release Readiness

## Contract

Document requirements, setup, `.env`, PostgreSQL, key locations/permissions,
Docker mapping, first startup, groups/devices, SSH testing, refresh data, safe
show/configuration workflows, topology, backups, stop/update, PostgreSQL
backup/restore, and SSH troubleshooting. Perform final tests/builds and create
a release checklist without adding major features.

## Outcome

The operator README, `docs/release-checklist.md`, updated architecture/plan,
and final validation were delivered. Later instruction cleanup moved the
durable agent guidance into this `docs/` tree.
