# Phase 06: Device Management UI

## Contract

Add explicit device refresh for facts, inventory, interfaces, VLANs, CDP, and
LLDP. Isolate/test fallback parsing. Build Device Detail, front panel, port
drawer, status, terminal, and connection-result components. Add Configuration
preview/confirmation scaffolding without executing commands on initial submit.

## Outcome

Structured refresh responses, parser fixtures, the polished Device Detail UI,
safe show terminal, and audit-only configuration confirmation were delivered.
